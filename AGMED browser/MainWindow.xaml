<Window x:Class="AGMED_browser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AGMED_browser"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Br0wser" 
        Height="800" Width="1200" 
        WindowStartupLocation="CenterScreen"
        Background="#FF252526">

    <Window.Resources>
        <!-- Modern Color Palette -->
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#FF252526"/>
        <SolidColorBrush x:Key="PrimaryAccentBrush" Color="#FF007ACC"/>
        <SolidColorBrush x:Key="SecondaryAccentBrush" Color="#FF3E3E42"/>
        <SolidColorBrush x:Key="TextBrush" Color="#FFF1F1F1"/>
        <SolidColorBrush x:Key="HoverBrush" Color="#FF3E3E42"/>
        <SolidColorBrush x:Key="PressedBrush" Color="#FF007ACC"/>

        <!-- Drop Shadow Effect -->
        <DropShadowEffect x:Key="DropShadowEffect" 
                         BlurRadius="10" 
                         ShadowDepth="2" 
                         Opacity="0.3"/>

        <!-- Modern Button Style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PressedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource SecondaryAccentBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Modern TabControl Style -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="{StaticResource SecondaryAccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4,4,0,0">
                            <ContentPresenter x:Name="content" 
                                            ContentSource="Header"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryAccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern StatusBar Style -->
        <Style TargetType="StatusBar">
            <Setter Property="Background" Value="{StaticResource SecondaryAccentBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Height" Value="24"/>
        </Style>

        <!-- Modern Separator Style -->
        <Style x:Key="ModernSeparator" TargetType="Separator">
            <Setter Property="Background" Value="#FF555555"/>
            <Setter Property="Margin" Value="8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Height="1" Background="{TemplateBinding Background}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Modern Tab Control -->
        <TabControl x:Name="tabControl" Grid.Row="0" SelectionChanged="TabControl_SelectionChanged">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Header}" Margin="0,0,5,0" VerticalAlignment="Center"/>
                        <Button Content="×" Width="16" Height="16" 
                                Click="CloseTab_Click" 
                                Tag="{Binding}"
                                Style="{StaticResource {x:Type Button}}"
                                Padding="0"
                                Margin="4,0,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>
        </TabControl>

        <!-- Modern Navigation Toolbar -->
        <Border Grid.Row="1" Background="{StaticResource SecondaryAccentBrush}" Padding="8,4">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="btnBack" Content="←" ToolTip="Back" Click="btnBack_Click"/>
                <Button x:Name="btnForward" Content="→" ToolTip="Forward" Click="btnForward_Click"/>
                <Button x:Name="btnRefresh" Content="↻" ToolTip="Refresh" Click="btnRefresh_Click"/>
                <Button x:Name="btnHome" Content="⌂" ToolTip="Home" Click="btnHome_Click"/>

                <TextBox x:Name="txtAddress" MinWidth="400" Margin="8,0" KeyDown="txtAddress_KeyDown"/>

                <Button x:Name="btnGo" Content="Go" Click="btnGo_Click" Margin="8,0"/>

                <Button x:Name="btnNewTab" Content="+" ToolTip="New Tab" Click="btnNewTab_Click"/>

                <Separator Style="{StaticResource ModernSeparator}"/>

                <Button x:Name="btnBookmark" Content="★" ToolTip="Add Bookmark" Click="btnBookmark_Click"/>
                <Button x:Name="btnShowBookmarks" Content="📚" ToolTip="Show Bookmarks" Click="btnShowBookmarks_Click"/>
            </StackPanel>
        </Border>

        <!-- WebView Content Area -->
        <Grid x:Name="webViewContainer" Grid.Row="2" Background="#FF1E1E1E"/>

        <!-- Modern Status Bar -->
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock x:Name="lblStatus" Text="Ready" Foreground="{StaticResource TextBrush}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <ProgressBar x:Name="progressBar" 
                            Width="150" 
                            Height="8" 
                            IsIndeterminate="False"
                            Foreground="{StaticResource PrimaryAccentBrush}"
                            Background="{StaticResource SecondaryAccentBrush}"/>
            </StatusBarItem>
        </StatusBar>

        <!-- Modern Bookmarks Flyout -->
        <Popup x:Name="bookmarksPopup" 
               Placement="Bottom" 
               PlacementTarget="{Binding ElementName=btnShowBookmarks}" 
               StaysOpen="False" 
               Width="300" 
               Height="400">
            <Border Background="#FF252526" 
                    BorderBrush="#FF555555" 
                    BorderThickness="1"
                    CornerRadius="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Background="{StaticResource PrimaryAccentBrush}" 
                            CornerRadius="4,4,0,0"
                            Padding="8">
                        <TextBlock Text="Bookmarks" 
                                   FontWeight="Bold" 
                                   Foreground="White"/>
                    </Border>

                    <ListBox x:Name="bookmarksList" 
                             Grid.Row="1" 
                             MouseDoubleClick="BookmarksList_MouseDoubleClick"
                             Background="Transparent"
                             BorderThickness="0">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="8,6"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="border" 
                                                    Background="{TemplateBinding Background}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="2">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#FF3E3E42"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#FF007ACC"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Title}" 
                                               Foreground="{StaticResource TextBrush}"
                                               TextTrimming="CharacterEllipsis"
                                               ToolTip="{Binding Url}"
                                               VerticalAlignment="Center"
                                               Width="220"/>
                                    <Button Content="×" 
                                            Width="20"
                                            Margin="5,0,0,0"
                                            Click="DeleteBookmark_Click"
                                            Tag="{Binding}"
                                            ToolTip="Delete bookmark"
                                            Style="{StaticResource {x:Type Button}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Popup>
    </Grid>
</Window>